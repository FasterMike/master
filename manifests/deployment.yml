apiVersion : apps/v1
kind: Deployment
metadata:
  name: fastermikemaster
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fast
  template:
    metadata:
      labels:
        app: fast
    spec:
      containers:
        - name: fast
          image: aakmiha1.azurecr.io/fast
          ports:
          - containerPort: 80

